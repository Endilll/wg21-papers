---
title: Grammar non-terminals for postfix-expressions
document: P4176R0
date: 2026-04-20
audience:
  - Core Working Group
author:
  - name: Vlad Serebrennikov
    email: <serebrennikov.vladislav@gmail.com>
---

# Abstract

[expr.post]{.sref} lists all the grammar at the top, while the rest of the subclause resorts to either quoting the grammar or describing it with words. This paper aims to improve the situation, introducing new nonterminals and putting their definitions in the respective subclauses.

# Revision history

R0:

- Initial revision.

# Approach

- Move grammar definitions of various forms of #postfix-expression# from [expr.post.general]{.sref} to their respective subclauses, defining new non-terminals.
- Define #postfix-expression#s in terms of new non-terminals.
- Make adjustments to the surrounding wording to improve integration between grammar and description of the semantics.
- Replace usages of regular font terms with their corresponding grammar productions, when grammatical parts are referred to. For instance, usages of "function call" that refer to its #postfix-expression# are replaced with #function-call-expression#.

No changes to behavior of programs are intended.

```{=html}
<div id="switches">
  <div>
    <label for=hide_rm>Hide removals:</label>
    <input type="checkbox" id="hide_rm"></input>
  </div>
  <div style="float:right">
    <label for=strikethrough_rm>Strikethrough:</label>
    <input type="checkbox" id="strikethrough_rm" checked="true"></input>
  </div>
</div>
```

# Proposed wording

## Postfix expressions [expr.post]{.sref .unnumbered}

### General [expr.post.general]{.sref .unnumbered}

Change [expr.post.general]{.sref} paragraph 1 as follows:

> [1]{.pnum} Postfix expressions group left-to-right.
>
>> | _postfix-expression_:
>>> | _primary-expression_
>> ::: rm
>>> | _postfix-expression_ `[` _expression-list~opt~_ `]`
>> :::
>> ::: add
>>> | _subscript-expression_
>> :::
>> ::: rm
>>> | _postfix-expression_ `(` _expression-list~opt~_ `)`
>> :::
>> ::: add
>>> | _function-call-expression_
>> :::
>> ::: rm
>>> | _simple-type-specifier_ `(` _expression-list~opt~_ `)`
>>> | _typename-specifier_ `(` _expression-list~opt~_ `)`
>>> | _simple-type-specifier_ _braced-init-list_
>>> | _typename-specifier_ _braced-init-list_
>> :::
>> ::: add
>>> | _type-conversion-expression_
>> :::
>> ::: rm
>>> | _postfix-expression_ `.` `template`_~opt~_ _id-expression_
>>> | _postfix-expression_ `.` _splice-expression_
>>> | _postfix-expression_ `->` `template`_~opt~_ _id-expression_
>>> | _postfix-expression_ `->` _splice-expression_
>> :::
>> ::: add
>>> | _class-member-access-expression_
>> :::
>> ::: rm
>>> | _postfix-expression_ `++`
>> :::
>> ::: add
>>> | _post-increment-expression_
>> :::
>> ::: rm
>>> | _postfix-expression_ `--`
>> :::
>> ::: add
>>> | _post-decrement-expression_
>> :::
>> ::: rm
>>> | `dynamic_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>> ::: add
>>> | _dynamic-cast-expression_
>> :::
>> ::: rm
>>> | `static_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>> ::: add
>>> | _static-cast-expression_
>> :::
>> ::: rm
>>> | `reinterpret_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>> ::: add
>>> | _reinterpret-cast-expression_
>> :::
>> ::: rm
>>> | `const_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>> ::: add
>>> | _const-cast-expression_
>> :::
>> ::: rm
>>> | `typeid` `(` _expression_ `)`
>>> | `typeid` `(` _type-id_ `)`
>> :::
>> ::: add
>>> | _typeid-expression_
>> :::
>
>> | _expression-list_:
>>> | _initializer-list_

Remove [expr.post.general]{.sref} paragraph 2:

> ::: rm
> [2]{.pnum}
>
> ::: note
> The `>` token following the #type-id# in a `dynamic_cast`, `static_cast`, `reinterpret_cast`, or `const_cast` can be the product of replacing a `>>` token by two consecutive `>` tokens ([temp.names]{.sref}).
> :::
> :::

::: draftnote
A similar note is added to each kind of cast.
:::

### Subscripting [expr.sub]{.sref .unnumbered}

Change [expr.sub]{.sref} paragraph 1 as follows:

> [1]{.pnum} A _subscript expression_ is a [postfix expression followed by square brackets containing a possibly empty, comma-separated list of #initializer-clause#s that]{.rm} [#subscript-expression#, which has the following form]{.add}
>
> ::: add
>> | _subscript-expression_:
>>> | _postfix-expression_ `[` _expression-list~opt~_ `]`
> :::
>
> [The #expression-list#]{.add} constitute[s]{.add} the arguments to the subscript operator. The #postfix-expression# and the initialization of the object parameter ([dcl.fct]{.sref}) of any applicable subscript operator function ([over.sub]{.sref}) is sequenced before each expression in the #expression-list# and also before any default argument ([dcl.fct.default]{.sref}). The initialization of a non-object parameter of a subscript operator function `S`, including every associated value computation and side effect, is indeterminately sequenced with respect to that of any other non-object parameter of `S`.

### Function call [expr.call]{.sref .unnumbered}

Change [expr.call]{.sref} paragraph 1 as follows:

> [1]{.pnum} A [function call]{.rm} [_function call_]{.add} is a [postfix expression followed by parentheses containing a possibly empty, comma-separated list of initializer-clauses which]{.rm} [#function-call-expression#, which has the form]{.add}
>
>> ::: add
>> | _function-call-expression_:
>>> | _postfix-expression_ `(` _expression-list~opt~_ `)`
>> :::
>
> [The #expression-list#, if present,]{.add} constitute[s]{.add} the arguments to the function.
>
> ::: note
> If the [postfix expression]{.rm} [#postfix-expression#]{.add} is a function name, the appropriate function and the validity of the call are determined according to the rules in [over.match]{.sref}.
> :::
>
>  The [postfix expression]{.rm} [#postfix-expression# _E_]{.add} shall have function type or function pointer type. For a call to a non-member function or to a static member function, [the postfix expression]{.rm} [_E_]{.add} shall be either an lvalue that refers to a function (in which case the function-to-pointer standard conversion ([conv.func]{.sref}) is suppressed on [the postfix expression]{.rm} [_E_]{.add}), or a prvalue of function pointer type.

Change [expr.call]{.sref} paragraph 2 as follows:

> [2]{.pnum} If the selected function is non-virtual, or if the #id-expression# in the [class member access expression]{.rm} [#class-member-access-expression#]{.add} is a #qualified-id#, that function is called. Otherwise, its final overrider ([class.virtual]{.sref}) in the dynamic type of the object expression is called; such a call is referred to as a _virtual function call_.
>
> ::: note
> [. . .]
> :::

### Explicit type conversion (functional notation) [expr.type.conv]{.sref .unnumbered}

Change [expr.type.conv]{.sref} paragraph 1 as follows:

>> ::: add
>> | _type-conversion-expression_:
>>> | _simple-type-specifier_ `(` _expression-list~opt~_ `)`
>>> | _typename-specifier_ `(` _expression-list~opt~_ `)`
>>> | _simple-type-specifier_ _braced-init-list_
>>> | _typename-specifier_ _braced-init-list_
>> :::
>
> [1]{.pnum} [A #simple-type-specifier# or #typename-specifier# followed by a parenthesized optional #expression-list# or by a #braced-init-list# (the initializer)]{.rm} [A #type-conversion-expression#]{.add} constructs a value of the [specified]{.rm} type [specified by #simple-type-specifier# or #typename-specifier#]{.add} given the initializer[, which is #braced-init-list# or parentheses containing optional #expression-list#]{.add}. If the type is a placeholder for a deduced class type, it is replaced by the return type of the function selected by overload resolution for class template deduction for the remainder of this subclause. Otherwise, if the type contains a placeholder type, it is replaced by the type determined by placeholder type deduction ([dcl.type.auto.deduct]{.sref}). Let `T` denote the resulting type. Then:
>
> - [1.1]{.pnum} If the initializer is a parenthesized [single expression]{.rm} [#expression-list# with a single #initializer-clause#]{.add}, the type conversion expression is equivalent to the corresponding cast expression ([expr.cast]{.sref}).
> - [1.2]{.pnum} Otherwise, if `T` is _cv_ `void`, the initializer shall be `()` or `{}` (after pack expansion, if any), and the expression is a prvalue of type `void` that performs no initialization.
> - [1.3]{.pnum} Otherwise, if `T` is a reference type, the expression has the same effect as direct-initializing an invented variable `t` of type `T` from the initializer and then using `t` as the result of the expression; the result is an lvalue if `T` is an lvalue reference type or an rvalue reference to function type and an xvalue otherwise.
> - [1.4]{.pnum} Otherwise, the expression is a prvalue of type `T` whose result object is direct-initialized ([dcl.init]{.sref}) with the initializer.
>
> If the initializer is a parenthesized optional #expression-list#, `T` shall not be an array type.
>
> ::: example
> [. . .]
> :::

### Class member access [expr.ref]{.sref .unnumbered}

Change [expr.ref]{.sref} paragraph 1 as follows:

> ::: add
> [1]{.pnum} A _class member access expression_ is a #class-member-access-expression#, which has one of the following forms
>
>> | _class-member-access-expression_:
>>> | _postfix-expression_ `.` `template`_~opt~_ _id-expression_
>>> | _postfix-expression_ `.` _splice-expression_
>>> | _postfix-expression_ `->` `template`_~opt~_ _id-expression_
>>> | _postfix-expression_ `->` _splice-expression_
> :::
>
> [A postfix expression followed by a dot `.` or an arrow `->`, optionally followed by the keyword `template`, and then followed by an #id-expression# or a #splice-expression#, is a postfix expression.]{.rm}
>
> ::: note
> If the keyword `template` is used and followed by an #id-expression#, the unqualified name is considered to refer to a template ([temp.names]{.sref}). If a #simple-template-id# results and is followed by a `​::`​, the #id-expression# is a #qualified-id#.
> :::

Change footnote 45 referenced from [expr.ref]{.sref} paragraph 3:

> If the [class member access expression]{.rm} [#class-member-access-expression#]{.add} is evaluated, the subexpression evaluation happens even if the result is unnecessary to determine the value of the entire postfix expression, for example if the #id-expression# denotes a static member.

### Increment and decrement [expr.post.incr]{.sref .unnumbered}

Apply the following changes to the entire subclause [expr.post.incr]{.sref}:

> ::: add
>> | _post-increment-expression_
>>> | _postfix-expression_ `++`
>
>> | _post-decrement-expression_
>>> | _postfix-expression_ `--`
> :::
>
> [1]{.pnum} The value of [a postfix `++` expression]{.rm} [a #post-increment-expression#]{.add} is the value obtained by applying the lvalue-to-rvalue conversion ([conv.lval]{.sref}) to its operand[, which is the #postfix-expression#]{.add}.
>
> ::: note
> The value obtained is a copy of the original value.
> :::
>
>  The operand shall be a modifiable lvalue. The type of the operand shall be an arithmetic type other than _cv_ `bool`, or a pointer to a complete object type. An operand with volatile-qualified type is deprecated; see [depr.volatile.type]{.sref}. The value of the operand object is modified ([defns.access]{.sref}) as if it were the operand of the prefix `++` operator ([expr.pre.incr]{.sref}). The value computation of the `++` expression is sequenced before the modification of the operand object. With respect to an indeterminately-sequenced function call, the operation of postfix `++` is a single evaluation.
>
> ::: note
> Therefore, a function call cannot intervene between the lvalue-to-rvalue conversion and the side effect associated with any single postfix `++` operator.
> :::
>
> The result is a prvalue. The type of the result is the cv-unqualified version of the type of the operand.
>
> [2]{.pnum} The operand of [postfix `--`]{.rm} [#post-decrement-expression#]{.add} is decremented analogously to the [postfix `++` operator]{.rm} [#post-increment-expression#]{.add}.
>
> ::: note
> For prefix increment and decrement, see [expr.pre.incr]{.sref}.
> :::

### Dynamic cast [expr.dynamic.cast]{.sref .unnumbered}

Change [expr.dynamic.cast]{.sref} paragraph 1 as follows:

>> ::: add
>> | _dynamic-cast-expression_:
>>> | `dynamic_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>
> [1]{.pnum} [The result of the expression `dynamic_cast<T>(v)` is the result of converting the expression `v` to type `T`.]{.rm} [The result of a #dynamic-cast-expression# is the result of converting the #expression# `v` to the type `T` designated by the #type-id#.]{.add} `T` shall be a pointer or reference to a complete class type, or “pointer to _cv_ `void`”. The `dynamic_cast` operator shall not cast away constness ([expr.const.cast]{.sref}).
>
> ::: add
> ::: note
> The `>` token following the #type-id# can be the product of replacing a `>>` token by two consecutive `>` tokens ([temp.names]{.sref}).
> :::
> :::

### Type identification [expr.typeid]{.sref .unnumbered}

Change [expr.typeid]{.sref} paragraph 1 as follows:

>> ::: add
>> | _typeid-expression_
>>> | `typeid` `(` _expression_ `)`
>>> | `typeid` `(` _type-id_ `)`
>> :::
>
> [1]{.pnum} The result of a [`typeid` expression]{.rm} [#typeid-expression#]{.add} is an lvalue of static type `const std​::​type_info` ([type.info]{.sref}) and dynamic type `const std​::​type_info` or `const` _name_ where _name_ is an implementation-defined class publicly derived from `std​::​type_info` which preserves the behavior described in [type.info]{.sref}.^48^ The lifetime of the object referred to by the lvalue extends to the end of the program. Whether or not the destructor is called for the `std​::​type_info` object at the end of the program is unspecified.

### Static cast [expr.static.cast]{.sref .unnumbered}

Change [expr.static.cast]{.sref} paragraph 1 as follows:

>> ::: add
>> | _static-cast-expression_
>> |         `static_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>
> [1]{.pnum} [The result of the expression `static_cast<T>(v)` is the result of converting the expression `v` to type `T`.]{.rm} [The result of a #static-cast-expression# is the result of converting the #expression# `v` to the type `T` designated by the #type-id#.]{.add} If `T` is an lvalue reference type or an rvalue reference to function type, the result is an lvalue; if `T` is an rvalue reference to object type, the result is an xvalue; otherwise, the result is a prvalue.
>
> ::: add
> ::: note
> The `>` token following the #type-id# can be the product of replacing a `>>` token by two consecutive `>` tokens ([temp.names]{.sref}).
> :::
> :::

### Reinterpret cast [expr.reinterpret.cast]{.sref .unnumbered}

Change [expr.reinterpret.cast]{.sref} paragraph 1 as follows:

>> ::: add
>> | _reinterpret-cast-expression_
>>> | `reinterpret_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>
> [1]{.pnum} [The result of the expression `reinterpret_cast<T>(v)` is the result of converting the expression `v` to type `T`.]{.rm} [The result of a #reinterpret-cast-expression# is the result of converting the #expression# `v` to the type `T` designated by the #type-id#.]{.add} If `T` is an lvalue reference type or an rvalue reference to function type, the result is an lvalue; if `T` is an rvalue reference to object type, the result is an xvalue; otherwise, the result is a prvalue and the lvalue-to-rvalue, array-to-pointer, and function-to-pointer standard conversions are performed on the expression `v`. Conversions that can be performed explicitly using `reinterpret_cast` are listed below. No other conversion can be performed explicitly using `reinterpret_cast`.
>
> ::: add
> ::: note
> The `>` token following the #type-id# can be the product of replacing a `>>` token by two consecutive `>` tokens ([temp.names]{.sref}).
> :::
> :::

### Const cast [expr.const.cast]{.sref .unnumbered}

Change [expr.const.cast]{.sref} paragraph 1 as follows:

>> ::: add
>> | _const-cast-expression_
>>> | `const_cast` `<` _type-id_ `> ` `(` _expression_ `)`
>> :::
>
> [1]{.pnum} [The result of the `const_cast<T>(v)` is of type `T`.]{.rm} [The result of a #const-cast-expression# is the result of converting the #expression# `v` to the type `T` designated by the #type-id#.]{.add} If `T` is an lvalue reference to object type, the result is an lvalue; if `T` is an rvalue reference to object type, the result is an xvalue; otherwise, the result is a prvalue and the lvalue-to-rvalue, array-to-pointer, and function-to-pointer standard conversions are performed on the expression `v`. The temporary materialization conversion is not performed on `v`, other than as specified below. Conversions that can be performed explicitly using `const_cast` are listed below. No other conversion shall be performed explicitly using `const_cast`.
>
> ::: add
> ::: note
> The `>` token following the #type-id# can be the product of replacing a `>>` token by two consecutive `>` tokens ([temp.names]{.sref}).
> :::
> :::

## Overload resolution [over]{.sref .unnumbered}

### Function call syntax [over.match.call.general]{.sref .unnumbered}

Change [over.match.call.general]{.sref} paragraph 1 as follows:

> ::: rm
> [1]{.pnum} In a function call ([expr.call]{.sref})
>
>> | _postfix-expression_ `(` _expression-list~opt~_ `)`
>
> :::
> [if]{.rm} [If]{.add} the #postfix-expression# [of a #function-call-expression#]{.add} names at least one function or function template, overload resolution is applied as specified in [over.call.func]{.sref}. If the #postfix-expression# denotes an object of class type, overload resolution is applied as specified in [over.call.object]{.sref}.

### Call to designated function [over.call.func]{.sref .unnumbered}

Apply the following changes to the entire subclause [over.call.func]{.sref}:

> ::: rm
> [1]{.pnum} Of interest in [over.call.func]{.sref} are only those function calls in which the #postfix-expression# ultimately contains an #id-expression# or #splice-expression# that designates one or more functions. Such a #postfix-expression#, perhaps nested arbitrarily deep in parentheses, has one of the following forms:
>
>> | _postfix-expression_:
>>> | _postfix-expression_ `.` _id-expression_
>>> | _postfix-expression_ `.` _splice-expression_
>>> | _postfix-expression_ `->` _id-expression_
>>> | _postfix-expression_ `->` _splice-expression_
>>> | _id-expression_
>>> | _splice-expression_
>
> These represent two syntactic subcategories of function calls: qualified function calls and unqualified function calls.
> :::
>
> ::: add
> [2]{.pnum} _Qualified function call_ is a #function-call-expression# whose #postfix-expression#, perhaps nested arbitrarily deep in parentheses, has one of the following forms:
>
>> | _postfix-expression_ `.` _id-expression_
>> | _postfix-expression_ `.` _splice-expression_
>> | _postfix-expression_ `->` _id-expression_
>> | _postfix-expression_ `->` _splice-expression_
> :::
>
> In [qualified function]{.rm} [such]{.add} calls, the function is designated by an #id-expression# or #splice-expression# _E_ [preceded by an `->` or `.` operator]{.rm}. Since the construct `A->B` is generally equivalent to `(*A).B`, the rest of Clause [over]{.sref} assumes, without loss of generality, that all member function calls have been normalized to the form that uses an object and the `.` operator. Furthermore, Clause [over]{.sref} assumes that the #postfix-expression# that is the left operand of the `.` operator has type “_cv_ `T`” where `T` denotes a class.^94^ The set of candidate functions either is the set found by name lookup ([class.member.lookup]{.sref}) if _E_ is an #id-expression# or is the set determined as specified in [expr.prim.splice]{.sref} if _E_ is a #splice-expression#. The argument list [is the #expression-list# in the call]{.rm} [consists of the arguments of the #function-call-expression#]{.add} augmented by the addition of the left operand of the `.` operator in the normalized member function call as the implied object argument ([over.match.funcs]{.sref}).
>
> ::: add
> [3]{.pnum} _Unqualified function call_ is a #function-call-expression# whose #postfix-expression#, perhaps nested arbitrarily deep in parentheses, has one of the following forms:
>
>> | _id-expression_
>> | _splice-expression_
> :::
>
> In [unqualified function]{.rm} [such]{.add} calls, the function is designated by an #id-expression# or a #splice-expression# _E_. The set of candidate functions either is the set found by name lookup ([basic.lookup]{.sref}) if _E_ is an #id-expression# or is the set determined as specified in [expr.prim.splice]{.sref} if _E_ is a #splice-expression#. The set of candidate functions consists either entirely of non-member functions or entirely of member functions of some class `T`. In the former case or if _E_ is either a #splice-expression# or the address of an overload set, the argument list [is the same as the #expression-list# in the call]{.rm} [consists of the arguments of the #function-call-expression#]{.add}. Otherwise, the argument list [is the #expression-list# in the call]{.rm} [consists of the arguments of the #function-call-expression#]{.add} augmented by the addition of an implied object argument as in a qualified function call. [. . .]
>
> ::: example
> [. . .]
> :::

### Call to object of class type [over.call.object]{.sref .unnumbered}

Change [over.call.object]{.sref} paragraph 1 as follows:

> [1]{.pnum} If the #postfix-expression# _E_ [in the function call syntax]{.rm} [of a #function-call-expression#]{.add} evaluates to a class object of type “_cv_ `T`”, then the set of candidate functions includes at least the function call operators of `T`. The function call operators of `T` are the results of a search for the name `operator()` in the scope of `T`.

### Function call [over.call]{.sref .unnumbered}

Change [over.call]{.sref} paragraph 1 as follows:

> [1]{.pnum} A _function call operator function_ is a function named `operator()` that is a member function with an arbitrary number of parameters. It may have default arguments. For [an expression of the form]{.rm} [a #function-call-expression#]{.add}
>
>> | _postfix-expression_ `(` _expression-list~opt~_ `)`
>
> where the #postfix-expression# is of class type, the operator function is selected by overload resolution ([over.call.object]{.sref}). If a surrogate call function is selected, let _e_ be the result of invoking the corresponding conversion operator function on the #postfix-expression#;
>
> the expression is interpreted as
>
>> | [_e_]{.grammarserif} `(` _expression-list~opt~_ `)`
>
> Otherwise, the expression is interpreted as
>
>> | _postfix-expression_ `.` `operator ()` `(` _expression-list~opt~_ `)`

### Subscripting [over.sub]{.sref .unnumbered}

Change [over.sub]{.sref} paragraph 1 as follows:

> [1]{.pnum} A _subscripting operator_ function is a member function named `operator[]` with an arbitrary number of parameters. It may have default arguments. For [an expression of the form]{.rm} [a #subscript-expression#]{.add}
>
>> | _postfix-expression_ `[` _expression-list~opt~_ `]`
>
> the operator function is selected by overload resolution ([over.match.oper]{.sref}). If a member function is selected, the expression is interpreted as
>
>> | _postfix-expression_ `.` `operator` `[]` `(` _expression-list~opt~_ `)`

### Class member access [over.ref]{.sref .unnumbered}

Change [over.ref]{.sref} paragraph 1 as follows:

> [1]{.pnum} A _class member access operator function_ is a function named `operator->` that is a non-static member function taking no non-object parameters. For [an expression]{.rm} [a #class-member-access-expression#]{.add} of the form
>
>> | _postfix-expression_ `->` `template`_~opt~_ _id-expression_
>
> the operator function is selected by overload resolution ([over.match.oper]{.sref}), and the expression is interpreted as
>
>> | `(` _postfix-expression_ `.` `operator` `->` `()` `)` `->` `template`_~opt~_ _id-expression_
>
> Analogously, for [an expression]{.rm} [a #class-member-access-expression#]{.add} of the form
>
>> | _postfix-expression_ `->` _splice-expression_
>
> the operator function is selected by overload resolution, and the expression is interpreted as
>
>> | `(` _postfix-expression_ `.` `operator` `->` `()` `)` `->` _splice-expression_

## The rest of the core language wording

### One-defintion rule [basic.def.odr]{.sref .unnumbered}

Change [basic.def.odr]{.sref} paragraph 3 as follows:

> [3]{.pnum} An expression or conversion is _potentially evaluated_ unless it is an unevaluated operand ([expr.context]{.sref}), a subexpression thereof, or a conversion in an initialization or conversion sequence in such a context. The set of _potential results_ of an expression _E_ is defined as follows:
>
> - [. . .]
> - [3.3]{.pnum} If _E_ is a [class member access expression (7.6.1.5)]{.rm} [#class-member-access-expression#]{.add} of the form _E_~1~ `.` `template`~opt~ _E_~2~, where _E_~2~ designates a non-static data member or a direct base class relationship, the set contains the potential results of _E_~1~.
> - [3.4]{.pnum} If _E_ is a [class member access expression]{.rm} [#class-member-access-expression#]{.add} naming a static data member, the set contains the #id-expression# designating the data member.
> - [. . .]

### Unqualified name lookup [basic.lookup.unqual]{.sref .unnumbered}

Change [basic.lookup.unqual]{.sref} paragraph 4 as follows:

> [4]{.pnum} An _unqualified name_ is a name that does not immediately follow a #nested-name-specifier# or the `.` or `->` in a [class member access expression (7.6.1.5)]{.rm} [#class-member-access-expression#]{.add}, possibly after a `template` keyword or `~`. Unless otherwise specified, such a name undergoes unqualified name lookup from the point where it appears.

### Argument-dependent name lookup [basic.lookup.argdep]{.sref .unnumbered}

Change [basic.lookup.argdep]{.sref} paragraph 1 as follows:

> [1]{.pnum} When the #postfix-expression# in a [function call ([expr.call]{.sref})]{.rm} [#function-call-expression#]{.add} is an #unqualified-id#, and unqualified lookup ([basic.lookup.unqual]{.sref}) for the name in the #unqualified-id# does not find any
>
> - [1.1]{.pnum} declaration of a class member, or
> - [1.2]{.pnum} function declaration inhabiting a block scope, or
> - [1.3]{.pnum} declaration not of a function or function template
>
> then lookup for the name also includes the result of _argument-dependent lookup_ in a set of associated namespaces that depends on the types of the arguments (and for type template template arguments, the namespace of the template argument), as specified below.
>
> ::: example
> [. . .]
> :::

Change [basic.lookup.argdep]{.sref} paragraph 2 as follows:

> [2]{.pnum}
>
> ::: note
> For purposes of determining (during parsing) whether an expression is a #postfix-expression# [for a function call]{.rm} [of a #function-call-expression#]{.add}, the usual name lookup rules apply. In some cases a name followed by `<` is treated as a #template-name# even though name lookup did not find a #template-name# (see [temp.names]{.sref}). For example,
>
> ```cpp
> int h;
> void g();
> namespace N {
>   struct A {};
>   template <class T> int f(T);
>   template <class T> int g(T);
>   template <class T> int h(T);
> }
>
> int x = f<N::A>(N::A());        // OK, lookup of `f` finds nothing, `f` treated as template name
> int y = g<N::A>(N::A());        // OK, lookup of `g` finds a function, `g` treated as template name
> int z = h<N::A>(N::A());        // error: `h<` does not begin a #template-id#
> ```
>
> The rules have no effect on the syntactic interpretation of an expression. For example,
>
> ```cpp
> typedef int f;
> namespace N {
>   struct A {
>     friend void f(A &);
>     operator int();
>     void g(A a) {
>       int i = f(a);             // `f` is the typedef, not the friend function: equivalent to `int(a)`
>     }
>   };
> }
> ```
>
> Because the expression is not a [function call]{.rm} [#function-call-expression#]{.add}, argument-dependent name lookup does not apply and the friend function `f` is not found.
> :::

### Qualified name lookup [basic.lookup.qual.general]{.sref .unnumbered}

Change [basic.lookup.qual.general]{.sref} paragraph 2 as follows:

> [2]{.pnum} A _member-qualified name_ is the (unique) component name ([expr.prim.id.unqual]{.sref}), if any, of
>
>  - [2.1]{.pnum} an #unqualified-id# or
>  - [2.2]{.pnum} a #nested-name-specifier# of the form #type-name# `::` or #namespace-name# `::`
>
> in the #id-expression# of a [class member access expression ([expr.ref]{.sref})]{.rm} [#class-member-access-expression#]{.add}. [. . .]
>
> ::: note
> [. . .]
> :::
> ::: example
> [. . .]
> :::

### Dynamic storage duration [basic.stc.dynamic]{.sref .unnumbered}

Change [basic.stc.dynamic.general]{.sref} paragraph 2 as follows:

> [2]{.pnum} [. . .]
>
> ::: note
> he implicit declarations do not introduce the names `std`, `std::size_t`, `std::align_val_t`, or any other names that the library uses to declare these names. Thus, a #new-expression#, #delete-expression#, or [function call]{.rm} [#function-call-expression#]{.add} that refers to one of these functions without importing or including the header `<new>` ([new.syn]{.sref}) or importing a C++ library module ([std.modules]{.sref}) is well-formed. However, referring to `std` or `std::size_t` or `std::align_val_t` is ill-formed unless a standard library declaration ([cstddef.syn]{.sref}, [new.syn]{.sref}, [std.modules]{.sref}) of that name precedes ([basic.lookup.general]{.sref}) the use of that name.
> :::
>
> [. . .]

### Sequential execution [intro.execution]{.sref .unnumbered}

Change [intro.execution]{.sref} paragraph 11 as follows:

> [11]{.pnum} When invoking a function _f_ (whether or not the function is inline), every argument expression and the [postfix expression]{.rm} [#postfix-expression#]{.add} designating _f_ are sequenced before every precondition assertion of _f_ ([dcl.contract.func]{.sref}), which in turn are sequenced before every expression or statement in the body of _f_, which in turn are sequenced before every postcondition assertion of _f_.

### Names [expr.prim.id.general]{.sref .unnumbered}

Change [expr.prim.id.general]{.sref} paragraph 2 as folows:

> [2]{.pnum} If an #id-expression# _E_ denotes a non-static non-type member of some class `C` at a point where the current class ([expr.prim.this]{.sref}) is `X` and
>
> - [2.1]{.pnum} _E_ is potentially evaluated or `C` is `X` or a base class of `X`, and
> - [2.2]{.pnum} _E_ is not the #id-expression# of a [class member access expression ([expr.ref]{.sref})]{.rm} [#class-member-access-expression#]{.add}, and
> - [2.3]{.pnum} _E_ is not the #id-expression# of a #reflect-expression# ([expr.reflect]{.sref}), and
> - [2.4]{.pnum} if _E_ is a #qualified-id#, _E_ is not the un-parenthesized operand of the unary `&` operator ([expr.unary.op]{.sref}),
>
> the #id-expression# is transformed into a class member access expression using `(*this)` as the object expression. If this transformation occurs in the predicate of a precondition assertion of a constructor of `X` or a postcondition assertion of a destructor of `X`, the expression is ill-formed.
>
> ::: note
> If `C` is not `X` or a base class of `X`, the class member access expression is ill-formed. Also, if the #id-expression# occurs within a static or explicit object member function, the class member access is ill-formed.
> :::
>
> This transformation does not apply in the template definition context ([temp.dep.type]{.sref}).
>
> ::: example
> [. . .]
> :::

### Destruction [expr.prim.id.dtor]{.sref .unnumbered}

Change [expr.prim.id.dtor]{.sref} paragraph 2 as follows:

> [2]{.pnum} If the #id-expression# names a pseudo-destructor, `T` shall be a scalar type and the #id-expression# shall appear as the right operand of a [class member access ([expr.ref]{.sref})]{.rm} [#class-member-access-expression#]{.add} that forms the #postfix-expression# of a [function call ([expr.call]{.sref})]{.rm} [#function-call-expression#]{.add}.

### Expression splicing [expr.prim.splice]{.sref .unnumbered}

Change [expr.prim.splice]{.sref} paragraph 4 as follows:

> [4]{.pnum} For a #splice-expression# of the form template #splice-specialization-specifier#, the #splice-specifier# of the #splice-specialization-specifier# shall designate a template _T_.
>
> - [. . .]
>
> ::: note
> Class members are accessible from any point when designated by #splice-expressions# ([class.access.base]{.sref}). A [class member access expression (7.6.1.5)]{.rm} [#class-member-access-expression#]{.add} whose right operand is a #splice-expression# is ill-formed if the left operand (considered as a pointer) cannot be implicitly converted to a pointer to the designating class of the right operand.
> :::

### Comma operator [expr.comma]{.sref .unnumbered}

Change [expr.comma]{.sref} paragraph 2 as follows:

> [2]{.pnum}
>
> ::: note
> In contexts where the comma token is given special meaning (e.g., [function calls (7.6.1.3)]{.rm} [#function-call-expression#s]{.add}, [subscript expressions (7.6.1.2)]{.rm} [#subscript-expression#s]{.add}, [lists of initializers (9.5)]{.rm} [#initialzer-list#s]{.add}, or #template-argument-list#s ([temp.names]{.sref})), the comma operator as described in this subclause can appear only in parentheses.
>
> ::: example
> [. . .]
> :::
> :::

### Core constant expressions [expr.const.core]{.sref .unnumbered}

Change [expr.const.core]{.sref} paragraph 2 as follows:

> [2]{.pnum} An expression _E_ is a _core constant expression_ unless the evaluation of _E_, following the rules of the abstract machine ([intro.execution]{.sref}), would evaluate one of the following:
>
> - [2.1]{.pnum} `this` ([expr.prim.this]{.sref}), except
>   - [. . .]
>   - [2.1.2]{.pnum} when appearing as the #postfix-expression# of an implicit or explicit [class member access expression ([expr.ref]{.sref})]{.rm} [#class-member-access-expression#]{.add};
>   - [. . .]
> - [. . .]
> - [2.18]{.pnum} an invocation of a destructor ([class.dtor]{.sref}) or a [function call]{.rm} [#function-call-expression#]{.add} whose #postfix-expression# names a pseudo-destructor ([expr.call]{.sref}), in either case for an object whose lifetime did not begin within the evaluation of _E_;
> - [. . .]

### Default arguments [dcl.fct.default]{.sref .unnumbered}

Change [dcl.fct.default]{.sref} paragraph 9 as follows:

> [9]{.pnum} A default argument is evaluated each time the function is called with no argument for the corresponding parameter. A parameter shall not appear as a potentially evaluated expression in a default argument.
>
> ::: note
> [. . .]
> :::
> ::: example
> [. . .]
> :::
>
> A non-static member shall not be designated in a default argument unless
>
> - [9.1]{.pnum} it is designated by the #id-expression# or #splice-expression# of a [class member access expression (7.6.1.5)]{.rm} [#class-member-access-expression#]{.add},
> - [. . .]
>
> [. . .]

### Dependent names [temp.dep.general]{.sref .unnumbered}

Change [temp.dep.general]{.sref} paragraph 2 as follows:

> [2]{.pnum} A [dependent call]{.rm} [_dependent call_]{.add} is [an expression]{.rm} [a #function-call-expression#]{.add}, possibly formed as a non-member candidate for an operator ([over.match.oper]{.sref}), [of the form:]{.rm}
>
> ::: rm
>> | _postfix-expression_ `(` _expression-list~opt~_ `)`
> :::
>
> [where the]{.rm} [whose]{.add} #postfix-expression# is an #unqualified-id# and
>
> - [2.1]{.pnum} any of the expressions in the #expression-list# is a pack expansion ([temp.variadic]{.sref}), or
> - [2.2]{.pnum} any of the expression or #brace-init-list#s in the #expression-list# is type-dependent ([temp.dep.expr]{.sref}), or
> - [2.3]{.pnum} the #unqualified-id# is a #template-id# in which any of the template arguments depends on a template parameters.
>
> The component name of an #unqualified-id# ([expr.prim.id.unqual]{.sref}) is dependent if
>
> - [2.4]{.pnum} it is a #conversion-function-id# whose #conversion-type-id# is dependent, or
> - [2.5]{.pnum} it is `operator=` and the current class is a templated entity, or
> - [2.6]{.pnum} the #unqualified-id# is the #postfix-expression# in a dependent call.
>
> ::: note
> [. . .]
> :::

### Type-dependent expressions [temp.dep.expr]{.sref .unnumbered}

Do not change [temp.dep.expr]{.sref} paragraph 3.

Change [temp.dep.expr]{.sref} paragraph 4 as follows:

> [4]{.pnum} Expressions of the following forms are never type-dependent (because the type of the expression cannot be dependent):
>
> > | _literal_
> > | `sizeof` _unary-expression_
> > | `sizeof` `(` _type-id_ `)`
> > | `sizeof` `...` `(` _identifier_ `)`
> > | `alignof` `(` _type-id_ `)`
> > | [`typeid` `(` _expression_ `)`]{.rm}
> > | [`typeid` `(` _type-id_ `)`]{.rm}
> > | [_typeid-expression_]{.add}
> > | `::`_~opt~_ `delete` _cast-expression_
> > | `::`_~opt~_ `delete` `[` `]` _cast-expression_
> > | `throw` _assignment-expression~opt~_
> > | `noexcept` `(` _expression_ `)`
> > | _requires-expression_
> > | _reflect-expression_
>
> ::: note
> For the standard library macro `offsetof`, see [support.types]{.sref}.
> :::

### Value-dependent expression [temp.dep.constexpr]{.sref .unnumbered}

Do not change [temp.dep.constexpr]{.sref} paragraphs 2 and 3.

### Deducing template arguments from a type [temp.deduct.type]{.sref .unnumbered}

Change [temp.deduct.type]{.sref} paragraph 23 as follows:

> [23]{.pnum} The #template-argument# corresponding to a template template parameter is deduced from the type of the #template-argument# of a class template specialization used in the argument list of a function call [([over.call.func]{.sref})]{.add}.
>
> ::: example
> [. . .]
> :::

### Exception specifications [except.spec]{.sref .unnumbered}

Change [except.spec]{.sref} paragraph 5 as follows:

> [5]{.pnum} An expression _E_ is _potentially-throwing_ if
>
> - [5.1]{.pnum} _E_ is a [function call ([expr.call]{.sref})]{.rm} [#function-call-expression#]{.add} whose #postfix-expression# has a function type, or a pointer-to-function type, with a potentially-throwing exception specification, or
> - [. . .]

## Library wording

### Algorithm function objects [alg.func.obj]{.sref .unnumbered}

Change [alg.func.obj]{.sref} paragraph 2 as follows:

> [2]{.pnum} For an algorithm function object `o`, let _S_ be the corresponding set of function templates. Then for any sequence of arguments `args . . . `, `o(args . . . )` is expression-equivalent to `s(args . . . )`, where the result of name lookup for `s` is the overload set _S_.
>
> ::: note
> Algorithm function objects are not found by argument-dependent name lookup ([basic.lookup.argdep]{.sref}). When found by unqualified name lookup ([basic.lookup.unqual]{.sref}) for the #postfix-expression# in a [function call ([expr.call]{.sref})]{.rm} [#function-call-expression#]{.add}, they inhibit argument-dependent name lookup.
>
> ::: example
> ```cpp
> void foo() {
>   using namespace std::ranges;
>   std::vector<int> vec{1,2,3};
>   find(begin(vec), end(vec), 2);             // #1
> }
> ```
> The [function call expression]{.rm} [#function-call-expression#]{.add} at #1 invokes `std::ranges::find`, not `std::find`.
> :::
> :::

### Creation [support.srcloc.cons]{.sref .unnumbered}

Change [support.srcloc.cons]{.sref} paragraph 1 as follows:

> ```cpp
> static consteval source_location current() noexcept;
> ```
>
>> [1]{.pnum} _Returns_:
>>
>> - [1.1]{.pnum} When invoked by a [function call]{.rm} [#function-call-expression#]{.add} whose #postfix-expression# is a (possibly parenthesized) #id-expression# naming `current`, returns a `source_location` with an implementation-defined value. [. . .]
>> - [. . .]

### Function objects [function.objects.general]{.sref .unnumbered}

Change [function.objects.general]{.sref} paragraph 1 as follows:

> [1]{.pnum} A function object type is an object type ([basic.types.general]{.sref}) that can be the type of the #postfix-expression# in a [function call ([expr.call]{.sref}, [over.match.call]{.sref})]{.rm} [#function-call-expression#]{.add}.^174^